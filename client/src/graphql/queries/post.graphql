query Post($postId: Float!) {
  post(id: $postId) {
    id
    title
    content
    hasVoted
    isOwner
    points
    joinStatus
    creator {
      username
    }
    club {
      id
      name
      description
      dateCreated
    }
  }
}
