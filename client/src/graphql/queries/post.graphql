query Post($postId: Float!) {
  post(id: $postId) {
    id
    title
    content
    hasVoted
    isOwner
    points
    joinStatus
    creator {
      username
    }
    community {
      id
      name
      description
      dateCreated
    }
  }
}
