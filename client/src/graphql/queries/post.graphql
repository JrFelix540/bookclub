query Post($postId: Float!) {
  post(id: $postId) {
    id
    title
    content
    hasVoted
    isOwner
    points
    joinStatus
    creator {
      id
      username
    }
    club {
      id
      name
      description
      dateCreated
    }
  }
}
